<h2>マイページ</h2>
<p>名前：<%= @user.name %></p>
<p>メール：<%= @user.email %></p>

<h3>あなたの投稿一覧</h3>
<ul>
  <% @user.posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
