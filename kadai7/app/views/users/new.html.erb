<h2>新規登録</h2>
<%= form_with model: @user, local: true do |f| %>
  <p>名前：<%= f.text_field :name %></p>
  <p>メール：<%= f.email_field :email %></p>
  <p>パスワード：<%= f.password_field :password %></p>
  <%= f.submit "登録" %>
<% end %>
<%= link_to "ログイン", login_path %>